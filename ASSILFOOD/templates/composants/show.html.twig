{% extends 'base.html.twig' %}

{% block title %}Composants{% endblock %}

{% block body %}
    <h1>Composants</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdComposants</th>
                <td>{{ composant.idComposants }}</td>
            </tr>
            <tr>
                <th>NomComposants</th>
                <td>{{ composant.nomComposants }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('composants_index') }}">back to list</a>

    <a href="{{ path('composants_edit', {'idComposants': composant.idComposants}) }}">edit</a>

    {{ include('composants/_delete_form.html.twig') }}
{% endblock %}
