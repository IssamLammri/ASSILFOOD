<form method="post" action="{{ path('appendice_delete', {'idAppendice': appendice.idAppendice}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ appendice.idAppendice) }}">
    <button class="btn">Delete</button>
</form>
