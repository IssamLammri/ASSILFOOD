{% extends 'base.html.twig' %}

{% block title %}Edit Reaps{% endblock %}

{% block body %}
    <h1>Edit Reaps</h1>

    {{ include('reaps/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('reaps_index') }}">back to list</a>

    {{ include('reaps/_delete_form.html.twig') }}
{% endblock %}
