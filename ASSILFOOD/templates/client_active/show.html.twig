{% extends 'base.html.twig' %}

{% block title %}ClientActive{% endblock %}

{% block body %}
    <h1>ClientActive</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Email</th>
                <td>{{ client_active.email }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ client_active.password }}</td>
            </tr>
            <tr>
                <th>NumeroIdentitã©</th>
                <td>{{ client_active.numeroIdentitã© }}</td>
            </tr>
            <tr>
                <th>TypeIdentitã©</th>
                <td>{{ client_active.typeIdentitã© }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('client_active_index') }}">back to list</a>

    <a href="{{ path('client_active_edit', {'idClient': client_active.idClient}) }}">edit</a>

    {{ include('client_active/_delete_form.html.twig') }}
{% endblock %}
